# === SECRETS & CONFIG INSTANCE ===
core/config/common.config.php
data/jeedom_encryption.key

# === DONNÉES UTILISATEUR (instance-spécifique) ===
data/
backup/
log/

# === DB DUMPS ===
*.sql
DB_backup.sql

# === RUNTIME / TEMPORAIRE ===
tmp/
cache.tar.gz
*.tar.gz
*.tar

# === PLUGINS (instance-spécifique) ===
plugins/

# === BUILD DEPS (npm install les recrée) ===
node_modules/

# === OUTILS AUDIT/REFACTORING (internes) ===
prompts/
audits/
.codex-jeedom-patches/
desktop/css/purged/
desktop/css/purged-strict/
desktop/php/cssOverview.php

# === FICHIERS SERVEUR (ne font pas partie du projet web) ===
.ssh/
.bashrc
.profile
.bash_history
.bash_logout
.tmb/
.wget-hsts

# === OS ===
.DS_Store
Thumbs.db
*.swp
*~
.env
